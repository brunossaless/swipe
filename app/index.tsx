import { Ball } from "@/components/Ball";
import { View } from "react-native";

export default function Index() {
  return (
    <View
      style={{
        flex: 1,
      }}
    >
      <Ball />
    </View>
  );
}
